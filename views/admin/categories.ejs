<%- layout('admin/layout', { 
    pageTitle: 'Categories', 
    currentPage: 'categories',
    user: user 
}) %>

<div class="content-card">
    <div class="card-header">
        <h2><i class="fas fa-list"></i> All Categories</h2>
        <button onclick="showAddModal()" class="btn btn-primary">
            <i class="fas fa-plus-circle"></i> Add Category
        </button>
    </div>
    
    <div class="table-responsive">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Icon</th>
                    <th>Name (EN)</th>
                    <th>Slug</th>
                    <th>Articles</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (categories && categories.length > 0) { %>
                    <% categories.forEach(cat => { %>
                        <tr>
                            <td>#<%= cat.id %></td>
                            <td><i class="<%= cat.icon || 'fas fa-tag' %>" style="color: #dc2626;"></i></td>
                            <td><strong><%= cat.name_en %></strong></td>
                            <td><code><%= cat.slug %></code></td>
                            <td><span class="status-badge status-published"><%= cat.news_count || 0 %></span></td>
                            <td>
                                <span class="status-badge <%= cat.is_active ? 'status-active' : 'status-inactive' %>">
                                    <%= cat.is_active ? 'Active' : 'Inactive' %>
                                </span>
                            </td>
                            <td class="action-buttons">
                                <button onclick="editCategory(<%= cat.id %>)" class="btn-edit">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button onclick="toggleCategory(<%= cat.id %>, <%= cat.is_active %>)" class="btn-warning" style="background: #f59e0b; color: white; padding:5px 10px; border:none; border-radius:5px;">
                                    <i class="fas fa-toggle-<%= cat.is_active ? 'on' : 'off' %>"></i>
                                </button>
                                <button onclick="deleteCategory(<%= cat.id %>)" class="btn-delete">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="7" style="text-align: center; padding: 30px;">
                            No categories found.
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>

<!-- Add Category Modal -->
<div id="addModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3><i class="fas fa-plus-circle"></i> Add New Category</h3>
            <span class="close" onclick="hideAddModal()">&times;</span>
        </div>
        <form onsubmit="addCategory(event)">
    <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
            <div class="form-group">
                <label>Name (English) *</label>
                <input type="text" id="name_en" class="form-control" required>
            </div>
            <div class="form-group">
                <label>Name (French)</label>
                <input type="text" id="name_fr" class="form-control">
            </div>
            <div class="form-group">
                <label>Name (Kinyarwanda)</label>
                <input type="text" id="name_rw" class="form-control">
            </div>
            <div class="form-group">
                <label>Slug (URL)</label>
                <input type="text" id="slug" class="form-control" placeholder="leave empty to auto-generate">
            </div>
            <div class="form-group">
                <label>Icon (Font Awesome class)</label>
                <input type="text" id="icon" class="form-control" placeholder="fas fa-tag">
            </div>
            <div class="form-group">
                <label>Description</label>
                <textarea id="description" class="form-control" rows="3"></textarea>
            </div>
            <div class="form-group">
                <label>Color</label>
                <input type="color" id="color" class="form-control" value="#dc2626">
            </div>
            <div class="form-group">
                <label>Order Position</label>
                <input type="number" id="order_position" class="form-control" value="0">
            </div>
            <button type="submit" class="btn btn-primary">Add Category</button>
            <button type="button" class="btn" onclick="hideAddModal()">Cancel</button>
        </form>
    </div>
</div>

<script>
function showAddModal() {
    document.getElementById('addModal').style.display = 'block';
}

function hideAddModal() {
    document.getElementById('addModal').style.display = 'none';
}

function addCategory(event) {
    event.preventDefault();
    
    const data = {
        name_en: document.getElementById('name_en').value,
        name_fr: document.getElementById('name_fr').value,
        name_rw: document.getElementById('name_rw').value,
        slug: document.getElementById('slug').value,
        icon: document.getElementById('icon').value,
        description: document.getElementById('description').value,
        color: document.getElementById('color').value,
        order_position: document.getElementById('order_position').value
    };
    
    fetch('/admin/categories/add', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': '<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>' },
        body: JSON.stringify(data)
    })
    .then(res => res.json())
    .then(data => {
        if (data.success) {
            location.reload();
        } else {
            alert('Error adding category');
        }
    });
}

function toggleCategory(id, current) {
    fetch('/admin/categories/toggle/' + id, { method: 'POST', headers: { 'CSRF-Token': '<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>' } })
        .then(res => res.json())
        .then(data => {
            if (data.success) {
                location.reload();
            }
        });
}

function deleteCategory(id) {
    if (confirm('Are you sure you want to delete this category? Articles in this category will be uncategorized.')) {
        fetch('/admin/categories/delete/' + id, { method: 'POST', headers: { 'CSRF-Token': '<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>' } })
            .then(res => res.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    alert('Error deleting category');
                }
            });
    }
}
</script>
